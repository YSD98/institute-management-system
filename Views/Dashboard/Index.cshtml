@{
    ViewData["Title"] = "Dashboard";
}

<div class="container mt-5">
    <h2 class="text-primary">Welcome to Your Dashboard 🎓</h2>
    <p class="lead">Manage your institute's data efficiently.</p>

    <div class="row mt-4">
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">👥 Users</h5>
                    <p class="card-text">View and manage registered users.</p>
                    <a href="/User" class="btn btn-outline-primary">Manage Users</a>
                </div>
            </div>
        </div>
        <!-- Add more dashboard cards as needed -->
    </div>
</div>
